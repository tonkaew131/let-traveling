{
  "name": "let-traveling",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "dotenv -- bun run --filter \"*\" dev",
    "format": "bun run --filter \"*\" format"
  },
  "dependencies": {
    "dotenv-cli": "^11.0.0"
  }
}
